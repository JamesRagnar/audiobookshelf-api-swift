Remaining items to complete:
- Video Track body
- Background Task body
- StartPlaybackSession.Parameters.DeviceInfo shared with start episode
- Bodies defined in Interface:
    - SearchChapters
    - SearchCovers
    - GetAuthSettings
    - A few more are defining it in the Response struct, but that might be OK
- QueuedTaskData body

- Models with manual decoding, but might not need it
    - RSSFeed

- User - readded auth and refresh tokens. Made optional. Check why socket events don't include.
